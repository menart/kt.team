{% extends 'base.twig' %}

{% block body %}
    <ul class="list-group">
        {% for item in list %}
            <li class="list-group-item"><a href="{{ item.href }}">{{ item.title }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}